# .env
DEBUG=true
POSTGRES_PASSWORD=supersecret          # keep in sync with docker-compose.yml
STRIPE_SECRET_KEY=sk_test_xxx          # real or dummy
STRIPE_WEBHOOK_SECRET=whsec_xxx
SECRET_KEY=$(openssl rand -hex 32)     # one-liner to generate if you like
